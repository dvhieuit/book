Swal.fire({
  title: 'Are you sure delete?',
  text: '<%= @catalog.name %>',
  type: 'warning',
  showCancelButton: true,
  confirmButtonColor: '#3085d6',
  cancelButtonColor: '#d33',
  confirmButtonText: '<%= link_to "Yes,delete", manage_catalog_path(@catalog), remote: true, method: :delete, class: "confirm-swal" %>'
})
